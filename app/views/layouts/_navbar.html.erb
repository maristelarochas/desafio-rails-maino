<nav class="bg-white shadow-lg">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">

      <div class="flex items-center space-x-4">
        <%= link_to root_path, class: "flex items-center space-x-2 group" do %>
          <svg class="w-8 h-8 text-indigo-600 group-hover:text-indigo-700 transition" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z"></path>
          </svg>
          <span class="text-xl font-bold text-gray-800 group-hover:text-indigo-600 transition">Movie Catalog</span>
        <% end %>
      </div>

      <div class="hidden md:flex items-center space-x-6">
        <%= link_to root_path, class: "flex items-center space-x-1 text-gray-700 hover:text-indigo-600 transition font-medium" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          <span>Início</span>
        <% end %>

        <%= link_to movies_path, class: "flex items-center space-x-1 text-gray-700 hover:text-indigo-600 transition font-medium" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z"></path>
          </svg>
          <span>Filmes</span>
        <% end %>

        <% if user_signed_in? %>
          <%= link_to new_movie_path, class: "flex items-center space-x-1 text-gray-700 hover:text-indigo-600 transition font-medium" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            <span>Adicionar Filme</span>
          <% end %>

          <div class="relative group">
            <button class="flex items-center space-x-2 text-gray-700 hover:text-indigo-600 transition px-3 py-2 rounded-lg hover:bg-gray-50">
              <div class="w-8 h-8 bg-indigo-600 rounded-full flex items-center justify-center text-white font-bold text-sm">
                <%= current_user.name.first.upcase %>
              </div>
              <span class="font-medium"><%= truncate(current_user.name, length: 15) %></span>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
>
            <div class="absolute right-0 mt-2 w-56 bg-white rounded-lg shadow-xl border border-gray-200 py-2 hidden group-hover:block z-50">
              <div class="px-4 py-3 border-b border-gray-200">
                <p class="text-sm font-semibold text-gray-900"><%= current_user.name %></p>
                <p class="text-xs text-gray-500 truncate"><%= current_user.email %></p>
              </div>

              <%= link_to movies_path, class: "flex items-center space-x-2 px-4 py-3 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                </svg>
                <span>Meus Filmes</span>
              <% end %>

              <%= link_to edit_user_registration_path, class: "flex items-center space-x-2 px-4 py-3 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
                <span>Meu Perfil</span>
              <% end %>

              <div class="border-t border-gray-200 my-2"></div>

              <%= button_to destroy_user_session_path, method: :delete, class: "flex items-center space-x-2 px-4 py-3 text-red-600 hover:bg-red-50 transition w-full text-left" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                </svg>
                <span>Sair</span>
              <% end %>
            </div>
          </div>
        <% else %>
          <%= link_to new_user_session_path, class: "flex items-center space-x-1 text-gray-700 hover:text-indigo-600 transition font-medium" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
            </svg>
            <span>Entrar</span>
          <% end %>

          <%= link_to new_user_registration_path, class: "bg-indigo-600 text-white px-6 py-2 rounded-lg hover:bg-indigo-700 transition font-medium shadow-sm hover:shadow-md" do %>
            Cadastrar
          <% end %>
        <% end %>
      </div>

      <!-- Mobile Menu Button -->
      <div class="md:hidden">
        <button id="mobile-menu-button" class="text-gray-700 hover:text-indigo-600 focus:outline-none p-2">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Menu (hidden by default) -->
    <div id="mobile-menu" class="hidden md:hidden pb-4 border-t border-gray-200 mt-2">
      <!-- Links Mobile -->
      <%= link_to root_path, class: "flex items-center space-x-2 py-3 px-2 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition rounded" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
        </svg>
        <span>Início</span>
      <% end %>

      <%= link_to movies_path, class: "flex items-center space-x-2 py-3 px-2 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition rounded" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z"></path>
        </svg>
        <span>Filmes</span>
      <% end %>

      <% if user_signed_in? %>
        <%= link_to new_movie_path, class: "flex items-center space-x-2 py-3 px-2 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition rounded" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
          <span>Adicionar Filme</span>
        <% end %>

        <div class="border-t border-gray-200 my-3"></div>

        <div class="px-2 py-3 bg-gray-50 rounded mb-2">
          <p class="text-sm font-semibold text-gray-900"><%= current_user.name %></p>
          <p class="text-xs text-gray-500"><%= current_user.email %></p>
        </div>

        <%= link_to edit_user_registration_path, class: "flex items-center space-x-2 py-3 px-2 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition rounded" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
          </svg>
          <span>Meu Perfil</span>
        <% end %>

        <%= button_to destroy_user_session_path, method: :delete, class: "flex items-center space-x-2 py-3 px-2 text-red-600 hover:bg-red-50 transition rounded w-full text-left" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
          <span>Sair</span>
        <% end %>
      <% else %>
        <div class="border-t border-gray-200 my-3"></div>

        <%= link_to new_user_session_path, class: "flex items-center space-x-2 py-3 px-2 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition rounded" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
          </svg>
          <span>Entrar</span>
        <% end %>

        <%= link_to new_user_registration_path, class: "flex items-center space-x-2 py-3 px-2 bg-indigo-600 text-white hover:bg-indigo-700 transition rounded font-medium justify-center mt-2" do %>
          Cadastrar
        <% end %>
      <% end %>
    </div>
  </div>
</nav>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');

    if (mobileMenuButton && mobileMenu) {
      mobileMenuButton.addEventListener('click', function() {
        mobileMenu.classList.toggle('hidden');
      });

      // Fechar menu ao clicar em um link
      const mobileLinks = mobileMenu.querySelectorAll('a');
      mobileLinks.forEach(link => {
        link.addEventListener('click', function() {
          mobileMenu.classList.add('hidden');
        });
      });
    }
  });
</script>
